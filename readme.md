### Simple laravel 5 rest api(crud) with vue.js on frontend

### Install:
- ```composer install```
- ```php artisan migrate```
- ```php artisan db:seed (if you need test data)```
- ```php artisan jwt:secret```